※ 個人学習のためにChatGPTを使用し、まとめたものです。

---

## 🔍 `janome` + `Word2Vec`（+関連モジュール）

この2つのライブラリは、このチャットボット(自然言語処理系AI)の**心臓部**にあたる存在です。  
**`gensim`（Word2Vecを含む）をインストールすると自動的に `numpy` などの依存ライブラリも一緒にインストール**されます。  

---

## 🔠 1. `janome`：日本語の形態素解析器

| 特徴 | 内容 |
|------|------|
| ✅ 純粋なPython製 | pipですぐ使えて、MeCabのような外部依存がない |
| ✅ 日本語対応 | 文を単語（トークン）に分けられる（＝形態素解析） |
| ✅ GUIアプリとも相性◎ | Pythonベースなので、Tkinterなどと簡単に連携可能 |

### 🔧 使い方例
```python
from janome.tokenizer import Tokenizer

tokenizer = Tokenizer()
text = "こんにちは、私はAIです。"

tokens = [token.surface for token in tokenizer.tokenize(text)]
print(tokens)  # ['こんにちは', '、', '私', 'は', 'AI', 'です', '。']
```

📌 `token.surface` で「表層形（見たままの形）」を取り出しています。

---

## 📐 2. `Word2Vec`（`gensim`ライブラリの一部）

| 特徴 | 内容 |
|------|------|
| ✅ 単語の意味を数値で表現 | 似ている単語は近いベクトルになる |
| ✅ 文章ベクトルも作れる | 平均を取ると文の意味ベクトルにできる |
| ✅ 類似度を測る | `cosine_similarity`でAIの判断が可能に |

### 🔧 学習例
```python
from gensim.models import Word2Vec

# 例：2つの文を学習させる（各文は単語リスト）
sentences = [["こんにちは", "私", "AI"], ["私は", "AI", "です"]]

model = Word2Vec(sentences, vector_size=100, window=5, min_count=1)

# ベクトル取得
vector = model.wv["AI"]  # 'AI'という単語のベクトル
```

### 📏 類似度計算の例
```python
from sklearn.metrics.pairwise import cosine_similarity
import numpy as np

vec1 = model.wv["こんにちは"]
vec2 = model.wv["こんばんは"]

score = cosine_similarity([vec1], [vec2])[0][0]
print(score)  # 1に近ければ意味が似ている
```

---

## 🔁 `janome` と `Word2Vec` を組み合わせると…

✅ 文をトークンに分割（janome）  
➡️ 各単語をベクトルに変換（Word2Vec）  
➡️ 平均を取って文ベクトル化（`np.mean`）  
➡️ ユーザーの入力と既存データを比較（`cosine_similarity`）  
➡️ 類似度が高いものを返す！

これが、このチャットボットが“それっぽく答えられる”仕組みです✨

---

## 💡 参考：インストールコマンドまとめ

```bash
pip install janome
pip install gensim
```

✅ `gensim` を入れると、自動的に `numpy`, `scipy`, `smart_open` , `scikit-learn`なども入ります。

---

## 🧪 どうやって意味を学ぶの？— Word2Vecの学習メカニズム

自然言語処理の世界では、「意味を持つ単語」や「似たような言葉どうしの関係性」を、**数値（ベクトル）で表現**する技術が使われます。その代表が `Word2Vec` です。

Word2Vecでは以下の2つの学習方法が代表的です：

| 方法 | 概要 |
|------|------|
| **CBOW (Continuous Bag of Words)** | 周囲の単語から中心の単語を予測する |
| **Skip-Gram** | 中心の単語から周囲の単語を予測する |

### 📘 Skip-Gram のイメージ
```
文： 私は AI です

中心の単語：「AI」
周辺の単語：「私」「です」

→「AI」を入力として、「私」「です」が来る確率を上げるように学習
```

このような処理を、数万〜数百万文のコーパスで何度も繰り返すことで、
- 「似たような使われ方をする単語」は、
- 「似たようなベクトル（数値の並び）」に変換されていくのです。

---

## 💡 キーワード：分布仮説と分散表現

この手法は、以下の原理に基づいています。

### 🧠 **分布仮説とは**
> 「言葉の意味は、その言葉が現れる文脈で決まる」

この仮説に従い、
- 「AI」「機械学習」「人工知能」は似た場面で使われやすく、
-  **意味の近いベクトル** として学習されるのです。

---

## 🌟 意味を持つ数値、分散表現（Distributed Representation）

Word2Vecは、1つの単語を100次元前後の**数値のベクトル**で表します（ベクトル＝配列）。  
これを **分散表現** と呼びます。

| 単語 | ベクトル例（100次元） |
|------|------------------------|
| 「AI」 | [0.22, -0.15, 0.63, ..., 0.08] |
| 「人工知能」 | [0.21, -0.14, 0.61, ..., 0.10] |

→ これらのベクトルを比べることで、**意味の近さ**を定量的に測れるようになります。

---

## 🧺 Bag of Words + sentence_vector

Word2Vecは単語単位の意味ベクトルを出してくれますが、文の意味を表現するためには、複数の単語のベクトルを組み合わせる必要があります。

その代表的な手法が：
> ✨ **sentence_vector = 各単語ベクトルの単純平均**

これは、文を「単語の袋（Bag of Words）」として扱い、順序を無視してベクトルの**中心点（重心）**を取る手法です。

📘 例：
```
["AI", "未来", "進化"] の各単語のベクトルを平均
→ 「AIの未来は進化する」の文ベクトルを近似的に表す
```

通文どうしの意味的近さを比較するには十分な精度を持ち、チャットボットなどでは実際に高い実用性を誇ります。

### ✅ sentence_vector（平均ベクトル）のメリット・デメリット

| 項目 | 内容 |
|------|------|
| ✅ メリット | 実装がシンプルで高速／短文中心の処理に向いている／小規模なコーパスでも機能する |
| ❌ デメリット | 単語の順序を考慮しない／よく出現する単語（例：「です」「ある」）に引っ張られやすい／文の構造が複雑な場合には弱い |

---

## 🚀 類似度とベクトル操作の可能性

Word2Vecで学習されたベクトルは、以下のようなことも可能です：

- 意味の近さ → `cosine_similarity("AI", "人工知能")`
- 類語の抽出 → `most_similar("王")` → ["女王", "皇帝", "将軍", ...]
- 概念操作 → `王 - 男 + 女 ≈ 女王`

これはすべて、**意味を数学的に捉える**というアプローチです。

---

## 🧭 ポイントまとめ

| キーワード | 意味 |
|------------|------|
| **分布仮説** | 「意味は使われる場所で決まる」という自然言語の基本法則 |
| **分散表現** | 単語や文を数百次元のベクトルで表す手法 |
| **Bag of Words + 平均** | 単語ベクトルを平均して文全体の意味をざっくり表す |
| **教師なし学習** | 「答え」を与えなくても、意味が自然とにじみ出てくる |

---

